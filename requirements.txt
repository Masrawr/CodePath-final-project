# --- Original game (Module 1) ---
streamlit>=1.21.0
altair<5
pytest

# --- Glitch Investigator AI (final project) ---
# Reasoning / detector (Gemini)
google-genai>=0.3.0

# Retrieval (RAG) — pure-Python TF-IDF by default (numpy only).
# sentence-transformers is an OPTIONAL semantic upgrade; uncomment to enable.
numpy>=1.24.0
# sentence-transformers>=2.2.0

# Specialized classifier (advanced feature) — local scikit-learn model
scikit-learn>=1.3.0
joblib>=1.3.0
