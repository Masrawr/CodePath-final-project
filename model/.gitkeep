# The specialized classifier (classifier.joblib) is saved here by
# src/train_classifier.py. The trained model is small (~6 KB) and committed so
# the app works right after clone; regenerate it any time with:
#   python3 src/train_classifier.py
